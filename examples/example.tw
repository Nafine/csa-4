hello := "hello";
a := 1;
b := 2;

def main() void {
    b = a + b * 3;
    b = a - b;
    b = a * b;
    b = a / b;

    if (a > b) {
        print(hello);
    }

    a = 1;
    b = 2;
    c := 3;

    print(a + b + (a * b * (a + c)));
}